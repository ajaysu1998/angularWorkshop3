import { SerachMoviePipe } from './serach-movie.pipe';

describe('SerachMoviePipe', () => {
  it('create an instance', () => {
    const pipe = new SerachMoviePipe();
    expect(pipe).toBeTruthy();
  });
});
